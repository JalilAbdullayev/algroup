---
const { label, placeholder, id, type = "text" } = Astro.props;
---

<div class="flex flex-col gap-2.5 lg:gap-3">
  <label class="text-base text-[#303030]" for={id}> {label}* </label>
  <input
    type={type}
    placeholder={placeholder}
    class="text-sm focus:outline-0 bg-[#F9F9F9] py-4 px-3 rounded-md placeholder:text-[#6F6F6F]"
    id={id}
  />
</div>
